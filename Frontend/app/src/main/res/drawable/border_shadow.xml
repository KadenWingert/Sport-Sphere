<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#11111000" /> <!-- Adjust alpha for a faded shadow -->
            <corners android:radius="10dp" />
            <padding
                android:bottom="4dp"
                android:right="4dp"
                android:top="4dp"
                android:left="4dp"/>
        </shape>
    </item>
    <!-- Border -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="2dp"
                android:color="#000000" /> <!-- Adjust shadow color here -->
            <corners android:radius="10dp" />
        </shape>
    </item>
    <!-- Background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#000000" /> <!-- Background color -->
            <corners android:radius="10dp" />
        </shape>
    </item>
</layer-list>
