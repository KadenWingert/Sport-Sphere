mutation CreateUser($email: String!, $password: String!, $first_name: String!, $last_name: String!) {
    createUser(email: $email, password: $password, first_name: $first_name, last_name: $last_name) {
        id
        email
        first_name
        last_name
    }
}